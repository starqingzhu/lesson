go mod 是什么？
    管理工具,管理项目引用的第三方包版本、自动识别用到的包、自动下载和管理包

为什么使用go mod？
    使用go mod 仓库中不用再上传依赖代码包，防止代码仓库过大浪费以及多个项目同时使用包时的浪费。
    管理引用包的版本，这一点gopath（src模式）和vendor 做不到。
    如果依赖gopath不同项目如果引用同一个包的不同版本，就会造成编译麻烦（gopath是go之前的默认策略，每个项目在运行时都要严格放在src目录下，而go mod不用）
    对于已经转移的包，可以用replace申明替换，不需要改代码
